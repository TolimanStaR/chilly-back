spring:
  application:
    name: place-svc-test

minio:
  endpoint: "${MINIO_ENDPOINT}"
  accessKey: "${MINIO_ACCESS_KEY}"
  secretKey: "${MINIO_SECRET_KEY}"
  bucket: "testbucket"


testcontainers:
  reuse:
    enabled: true